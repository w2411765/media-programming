# CAPONE ゲーム 実装状況

## 📋 概要
Java Swingで実装されているボードゲーム「CAPONE」の実装状況を整理したドキュメントです。

---

## ✅ 実装済み

### 🎨 ビュー（View）層

#### タイトル画面
- ✅ **TitlePanel.java** - 完全実装
  - 背景画像・タイトル画像の表示
  - プレイヤー名入力機能
  - 参加ボタン（最大4人まで）
  - ゲーム開始ボタン（4人揃ったら有効化）
  - プレイヤーリスト表示
  - 画面切り替え機能（GameBoardPanelへ）

#### ゲーム画面コンポーネント
- ✅ **GameBoardPanel.java** - 完全実装
  - レイアウト管理（BorderLayout）
  - 各パネルの配置とマージン設定

- ✅ **InfoPanel.java** - 完全実装
  - 背景画像表示（info.png）
  - 角丸レイアウト
  - カスタム描画

- ✅ **LogPanel.java** - 基本実装済み（確認必要）

- ✅ **MarketPanel.java** - 完全実装
  - 背景画像表示（brickwall.png）
  - 4方向のPlayerPanel配置（North/South/East/West）
  - CenterPanelの配置
  - 角丸レイアウト

- ✅ **CustomerPanel.java** - 基本実装済み
  - 背景画像表示（speakeasy.png）
  - 角丸レイアウト
  - ⚠️ コンテンツは「Customers」ラベルのみ（機能未実装）

- ✅ **PlayerPanel.java** - 完全実装
  - 4方向のパネル（North/South/East/West）
  - ShelfPanel（背景画像付き）
  - AlcoholPanelの配置（30個のスロット）

- ✅ **AlcoholPanel.java** - 存在確認済み（詳細未確認）

- ✅ **CenterPanel.java** - 存在確認済み（詳細未確認）

- ✅ **ToolPanel.java** - 存在確認済み（詳細未確認）

#### メインフレーム
- ✅ **MainFrame.java** - 部分実装
  - フルスクリーン表示
  - タイトル画面表示
  - ゲーム画面への切り替え
  - ⚠️ 以下のメソッドはスタブ（TODO）:
    - `updateRoundInfo()`
    - `updateAllPlayersState()`
    - `enableTradeUI()`
    - `enableServeUI()`
    - `showMessage()`
    - `showAuctionTruck()`
    - `showAuctionResult()`
    - `showTradeOfferDialog()`
    - `showGameOverDialog()`

#### ダイアログ
- ⚠️ **AuctionDialog.java** - 空ファイル（未実装）
- ⚠️ **TradeDialog.java** - 空ファイル（未実装）

---

### 🎮 コントローラー（Controller）層

- ✅ **GameManager.java** - 完全実装
  - ゲーム開始処理
  - フェーズ管理（ORDER_DISTRIBUTION → AUCTION → TRADE → SERVE → END）
  - 各フェーズの開始メソッド
  - イベントハンドラ（入札、取引、提供など）
  - ラウンド終了処理
  - ゲーム終了判定

- ✅ **AuctionManager.java** - 完全実装
  - オークション開始処理
  - 入札処理
  - オークション解決処理

- ✅ **TradeManager.java** - 完全実装
  - 取引フェーズ開始処理
  - 取引提案処理
  - 取引応答処理
  - 取引フェーズ終了処理

- ✅ **TradeProposal.java** - 完全実装
  - 取引内容のデータクラス
  - 酒・お金の交換条件を保持

- ⚠️ **AIController.java** - 存在確認済み（詳細未確認）

---

### 📦 モデル（Model）層

#### 基本モデル
- ✅ **Player.java** - 完全実装
  - 所持金管理
  - 酒の在庫管理（Map<AlcoholType, Integer>）
  - 注文カード管理
  - 注文達成判定・処理
  - 注文カード追加・削除

- ✅ **GameState.java** - 部分実装
  - ラウンド管理（getRound, nextRound）
  - フェーズ管理（getPhase, setPhase）
  - プレイヤー一覧管理
  - マーケット管理
  - ⚠️ 以下のメソッドはスタブ（TODO）:
    - `reset()` - ゲーム初期化
    - `dealCustomerCardsToAllPlayers()` - 客カード配布
    - `isGameOver()` - ゲーム終了判定
    - `getWinner()` - 勝者取得
    - `serveCustomer()` - 客への提供処理
    - `prepareTruckCardForAuction()` - オークション用トラックカード準備
    - `setBid()` - 入札額設定
    - `allBidsSubmitted()` - 全員入札済み判定
    - `resolveAuction()` - オークション解決
    - `applyTrade()` - 取引適用

- ✅ **Phase.java** - 完全実装
  - フェーズ列挙型（ORDER_DISTRIBUTION, AUCTION, TRADE, SERVE, END）

#### カードモデル
- ✅ **AlcoholCard.java** - 完全実装
  - 酒1本を表すカード
  - 酒の種類（AlcoholType）を保持

- ✅ **TruckCard.java** - 完全実装
  - 複数の酒をまとめたトラックカード
  - 積荷管理（Map<AlcoholType, Integer>）

- ✅ **OrderCard.java** - 完全実装
  - 客（注文）カード
  - 必要な酒の種類と本数
  - 報酬金額

- ✅ **Deck.java** - 完全実装
  - 汎用山札クラス
  - シャッフル機能
  - カードを引く機能

#### 列挙型
- ✅ **AlcoholType.java** - 完全実装
  - BEER, RUM, VODKA, GIN

---

### 🛠️ ユーティリティ

- ⚠️ **Constants.java** - 空ファイル（未実装）
- ⚠️ **ImageLoader.java** - 存在確認済み（詳細未確認）

---

### 🚀 エントリーポイント

- ✅ **Main.java** - 完全実装
  - MainFrameを起動

---

## ❌ 未実装・要実装

### 🔴 高優先度（ゲーム進行に必須）

#### GameState.java
1. **`reset()`** - ゲーム初期化
   - ラウンドを1にリセット
   - プレイヤーの状態を初期化（所持金、在庫、手札）
   - 山札の初期化・シャッフル

2. **`dealCustomerCardsToAllPlayers(int count)`** - 客カード配布
   - 各プレイヤーに指定枚数のOrderCardを配る
   - 山札（Deck<OrderCard>）が必要

3. **`isGameOver()`** - ゲーム終了判定
   - 勝利条件のチェック（例：所持金が一定額以上）
   - Constants.javaに勝利条件を定義する必要あり

4. **`getWinner()`** - 勝者取得
   - 最も所持金が多いプレイヤーを返す

5. **`serveCustomer(Player, OrderCard)`** - 客への提供
   - プレイヤーが注文を達成できるかチェック
   - 達成可能なら酒を消費し、報酬を追加
   - Player.completeOrder()を使用

6. **`prepareTruckCardForAuction()`** - オークション用トラックカード準備
   - 人数×4本の酒をまとめたTruckCardを作成
   - ランダムに酒の種類を決定

7. **`setBid(Player, int)`** - 入札額設定
   - プレイヤーの入札額を記録
   - 所持金をチェック（入札額以上必要）

8. **`allBidsSubmitted()`** - 全員入札済み判定
   - 全プレイヤーが入札したかチェック

9. **`resolveAuction()`** - オークション解決
   - 最高入札額のプレイヤーを決定
   - 勝者の所持金から入札額を減算
   - TruckCardの酒を勝者の在庫に追加
   - 勝者を返す

10. **`applyTrade(TradeProposal)`** - 取引適用
    - 提案者の酒・お金を減らし、相手に追加
    - 相手の酒・お金を減らし、提案者に追加
    - 在庫・所持金の整合性チェック

#### MainFrame.java
1. **`updateRoundInfo(int round)`** - ラウンド情報更新
   - InfoPanelにラウンド数を表示

2. **`updateAllPlayersState(List<Player>)`** - プレイヤー状態更新
   - 各PlayerPanelにプレイヤーの情報を表示
   - 所持金、在庫、手札を更新

3. **`enableTradeUI(boolean)`** - 取引UI有効化
   - 取引ボタンなどの有効/無効を切り替え

4. **`enableServeUI(boolean)`** - 提供UI有効化
   - 提供ボタンなどの有効/無効を切り替え

5. **`showMessage(String)`** - メッセージ表示
   - LogPanelにメッセージを表示

6. **`showAuctionTruck(TruckCard)`** - オークション用トラック表示
   - CenterPanelまたは専用パネルにTruckCardを表示

7. **`showAuctionResult(Player)`** - オークション結果表示
   - 勝者情報を表示

8. **`showTradeOfferDialog(Player, Player, TradeProposal)`** - 取引提案ダイアログ
   - TradeDialogを表示
   - 相手プレイヤーに取引内容を提示

9. **`showGameOverDialog(Player)`** - ゲーム終了ダイアログ
   - 勝者を表示
   - 再プレイまたは終了の選択

#### ダイアログ
1. **AuctionDialog.java** - オークションダイアログ
   - 入札額入力フィールド
   - 現在のTruckCard表示
   - 入札ボタン

2. **TradeDialog.java** - 取引ダイアログ
   - 取引内容の表示
   - 承諾/拒否ボタン
   - 取引提案作成UI（提案者側）

#### ユーティリティ
1. **Constants.java** - 定数定義
   - 初期所持金
   - 勝利条件（目標金額）
   - 最大ラウンド数
   - その他のゲーム設定値

---

### 🟡 中優先度（機能拡張）

1. **AIController.java** - AIプレイヤーの実装
   - 自動入札ロジック
   - 自動取引判断
   - 自動提供判断

2. **ImageLoader.java** - 画像読み込みユーティリティ
   - 画像キャッシュ機能
   - 画像リサイズ機能

3. **CustomerPanel.java** - 客カード表示機能
   - 各プレイヤーの手札（OrderCard）を表示
   - クリックで提供処理

4. **CenterPanel.java** - 中央パネルの機能実装
   - オークション中のTruckCard表示
   - その他のゲーム情報表示

5. **ToolPanel.java** - ツールパネルの機能実装
   - フェーズ表示
   - 操作ボタン（取引終了、提供終了など）

---

### 🟢 低優先度（改善・最適化）

1. エラーハンドリングの強化
2. ログ機能の拡充
3. 設定ファイル（config.properties）の活用
4. テストコードの作成
5. リファクタリング（コード整理）

---

## 📊 実装進捗率（概算）

- **ビュー層**: 約 70% （UIコンポーネントは完成、機能連携が未実装）
- **コントローラー層**: 約 90% （ロジックは完成、GameState依存の未実装部分あり）
- **モデル層**: 約 60% （基本クラスは完成、GameStateの主要メソッドが未実装）
- **全体**: 約 **65%**

---

## 🔗 依存関係

### 実装順序の推奨

1. **Constants.java** の実装（定数定義）
2. **GameState.java** の主要メソッド実装
   - `reset()` → `dealCustomerCardsToAllPlayers()` → `isGameOver()` → `getWinner()`
   - `prepareTruckCardForAuction()` → `setBid()` → `allBidsSubmitted()` → `resolveAuction()`
   - `serveCustomer()` → `applyTrade()`
3. **MainFrame.java** のスタブメソッド実装
4. **AuctionDialog.java** の実装
5. **TradeDialog.java** の実装
6. **TitlePanel.java** のゲーム開始処理実装（GameManagerとの連携）

---

## 📝 備考

- プロジェクト構造は MVC パターンに従っている
- 画像リソースは `/images/` 配下に配置されている
- ゲームは4人プレイを想定している
- フェーズベースのゲーム進行システムが実装されている
