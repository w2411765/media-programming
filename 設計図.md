# CAPONE ゲーム 設計図

リバースエンジニアリングによって作成した設計図です。

**最終更新**: 実装完了後の最新状態に更新済み（2024年）

> 📌 **実装状況**: 主要な機能はすべて実装済みです。詳細は [実装完了報告.md](実装完了報告.md) を参照してください。

---

## 1. クラス図

詳細は [class-diagram.puml](diagrams/class-diagram.puml) を参照

![クラス図](diagrams/class-diagram.png)

**スライド発表用（簡易・色付き）**: [class-diagram-simple.puml](diagrams/class-diagram-simple.puml)

---

## 2. シーケンス図

### 2.1 ゲーム開始シーケンス

詳細は [sequence-game-start.puml](diagrams/sequence-game-start.puml) を参照

![ゲーム開始シーケンス](diagrams/sequence-game-start.png)

### 2.2 オークションフェーズシーケンス

詳細は [sequence-auction.puml](diagrams/sequence-auction.puml) を参照

![オークションフェーズシーケンス](diagrams/sequence-auction.png)

### 2.3 取引フェーズシーケンス

詳細は [sequence-trade.puml](diagrams/sequence-trade.puml) を参照

![取引フェーズシーケンス](diagrams/sequence-trade.png)

### 2.4 提供フェーズシーケンス

詳細は [sequence-serve.puml](diagrams/sequence-serve.puml) を参照

![提供フェーズシーケンス](diagrams/sequence-serve.png)

---

## 3. 状態遷移図

詳細は [state-diagram.puml](diagrams/state-diagram.puml) を参照

![状態遷移図](diagrams/state-diagram.png)

---

## 4. 補足説明

### 4.1 クラス図について

- **モデル層**: ゲームの状態とデータを管理 ✅ 実装完了
  - `GameState`: ゲーム全体の状態管理（全メソッド実装済み）
  - `Player`: プレイヤーの状態（所持金、在庫、手札、拡張メソッド実装済み）
  - カードクラス: `AlcoholCard`, `TruckCard`, `OrderCard`
  - `Deck<T>`: 汎用山札クラス
  - `Constants`: ゲーム定数定義（実装済み）

- **コントローラー層**: ゲームロジックと進行管理 ✅ 実装完了
  - `GameManager`: 全体の進行管理
  - `AuctionManager`: オークションフェーズ管理（AuctionDialog連携実装済み）
  - `TradeManager`: 取引フェーズ管理（TradeDialog連携実装済み）

- **ビュー層**: UI表示とユーザーインタラクション ✅ 約85%完成
  - `MainFrame`: メインウィンドウ（全メソッド実装済み）
  - 各種パネル: ゲーム画面のコンポーネント（機能拡張済み）
  - ダイアログ: `AuctionDialog`, `TradeDialog`（完全実装済み）

### 4.2 シーケンス図について

4つの主要なシーケンスを表現（すべて実装済み）：
1. **ゲーム開始**: タイトル画面からゲーム画面への遷移、GameManager初期化
2. **オークションフェーズ**: AuctionDialogを使用した入札から解決まで
3. **取引フェーズ**: TradeDialogを使用した取引提案から成立まで
4. **提供フェーズ**: 客への提供からラウンド終了・ゲーム終了判定まで

### 4.3 状態遷移図について

`Phase` enumに基づく5つのフェーズ（すべて実装済み）：
1. **ORDER_DISTRIBUTION**: 注文カード配布（`dealCustomerCardsToAllPlayers()`実装済み）
2. **AUCTION**: オークション（`prepareTruckCardForAuction()`, `resolveAuction()`実装済み）
3. **TRADE**: プレイヤー間取引（`applyTrade()`実装済み）
4. **SERVE**: 客への提供（`serveCustomer()`実装済み）
5. **END**: ラウンド終了・ゲーム終了判定（`isGameOver()`, `getWinner()`実装済み）

各フェーズ内の詳細な状態遷移も表現しており、すべてのメソッドが実装済みです。

---

## 5. 実装状況

### ✅ 実装完了項目

- **GameState**: すべての主要メソッドが実装済み
  - `reset()`, `dealCustomerCardsToAllPlayers()`, `isGameOver()`, `getWinner()`
  - `serveCustomer()`, `prepareTruckCardForAuction()`, `setBid()`, `allBidsSubmitted()`
  - `resolveAuction()`, `applyTrade()`
  
- **Player**: 拡張メソッドが実装済み
  - `payMoney()`, `receiveMoney()`, `setMoney()`, `removeAlcohol()`
  
- **MainFrame**: すべてのスタブメソッドが実装済み
  - UIコンポーネントとの連携が完成
  
- **AuctionDialog**: 完全実装済み
  - 入札額入力、トラックカード表示、所持金チェック機能
  
- **TradeDialog**: 完全実装済み
  - 取引提案作成、表示、応答機能
  
- **Constants**: 定数定義が実装済み
  - 初期所持金、勝利条件、カード配布数など
  
- **UIコンポーネント**: 機能拡張済み
  - `LogPanel`: メッセージ表示機能
  - `InfoPanel`: ラウンド情報表示機能
  - `GameBoardPanel`: 各パネルへの参照保持

### ⚠️ 未実装・改善余地

- **AIController**: AIプレイヤーの実装（未実装）
- **ImageLoader**: 画像読み込みユーティリティ（未実装）
- **PlayerPanel**: プレイヤー情報の詳細表示（基本実装のみ）
- **CustomerPanel**: 客カードの表示・操作機能（基本実装のみ）
- **ToolPanel**: 操作ボタンの実装（基本実装のみ）

詳細は [実装完了報告.md](実装完了報告.md) を参照してください。
